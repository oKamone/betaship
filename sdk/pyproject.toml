[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "betaship"
version = "0.1.0"
requires-python = ">=3.11"
# 依存ライブラリ（requests/httpx/anthropic）は各サービスの requirements.txt に既存
dependencies = []

[tool.setuptools.packages.find]
include = ["betaship*"]
